#ifndef __CONVERSION_H__
#define __CONVERSION_H__

#include <string>
using namespace std;

bool   to_bool(const string& _value);
char   to_char(const string& _value);
int    to_int (const string& _value);
double to_real(const string& _value);

#endif
