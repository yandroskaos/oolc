#include <ast/structure_nodes.h>

using namespace AST;

ACCEPT_VISITOR(ProgramNode)
ACCEPT_VISITOR(InterfaceNode)
ACCEPT_VISITOR(PrototypeNode)
ACCEPT_VISITOR(ClassNode)
ACCEPT_VISITOR(AttributeNode)
ACCEPT_VISITOR(MethodNode)
